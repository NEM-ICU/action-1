name: my first workflow

on: push

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: welcome message
        run: echo "My firstgithub Actions job"

      - name: List Files
        run: ls

      - name: Read file
        run: cat README.md
